notifications: 
 slack: geekshubs:S32At4pC3DX58sqtloCFQXyz
    on_success: never
    on_failure: always
    template:
      - "%{repository_slug} (%{commit}) : %{message}"
      - "Build details: %{build_url}"         
language: python
dist: trusty
cache: false
python:
 - "3.6"
install: pip install -e .
script: pytest
